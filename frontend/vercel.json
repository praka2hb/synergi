{
    "framework": "nextjs",
    "buildCommand": "npm run build",
    "installCommand": "npm install",
    "outputDirectory": ".next",
    "functions": {
        "app/**/*.ts": {
            "maxDuration": 30
        },
        "app/**/*.tsx": {
            "maxDuration": 30
        }
    }
}