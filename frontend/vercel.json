{
    "framework": "nextjs",
    "buildCommand": "pnpm build",
    "installCommand": "pnpm install",
    "outputDirectory": ".next",
    "functions": {
        "app/**/*.ts": {
            "maxDuration": 30
        },
        "app/**/*.tsx": {
            "maxDuration": 30
        }
    }
}